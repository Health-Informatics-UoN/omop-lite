name: 'Dependency Review'
on: [pull_request]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: health-informatics-uon/workflows/.github/workflows/dependency-review.yml@1.4.6
    secrets: inherit
